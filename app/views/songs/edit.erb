<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>

    <form action="/songs/<%=@song.slug%>" method="post">
    <!-- <form action="/songs/<%=@song.slug%>" -->
    <input type = "hidden" id="hidden" name="_method" value="patch">
    <label>Create New Song:</label>
    <br></br>
    <input id="Name" type="text" name="song[name]" value = <%=@song.name %> >
    <br></br>

    <label>Add Existing Artist:</label>
    <br></br>
    <%@artists.each do |artist| %>
      <%=artist.name%><input id="<%=artist.name%>" type="checkbox" name="song[artist_id]" value= "<%=artist.id%>"
      <% if @song.artist_id == artist.id %>
          checked
        <% end %>
      >
      <br></br>
    <%end %>

    <h2><label>Add Existing Genre:</label></h2>
      <br></br>
      <%@genres.each do |genre| %>
        <%=genre.name%><input id="<%=genre.name%>" type="checkbox" name="song[genre_ids][]" value= "<%=genre.id%>"
        <% if @song.genre_ids.include?(genre.id) %>
                  checked
        <% end %>
        >
        <br></br>
      <%end %>

    <br></br>
    <label>Create New Artist:</label>
    <input type="text" id="Artist Name" name="artist[name]">

    <input type ="submit" value="Save">
    </form>


  </body>
</html>
